# このファイルを .env にコピーして値を入れる（.env はコミットされない）。
#
# GEMINI_API_KEY … 一括実測 tools/batch_visibility.py が使う。
#   本番の可視性チェッカー(Vercel)と同じ、**課金済みプロジェクト**のキーを入れる。
#   無料プロジェクトのキーでは検索グラウンディングが使えず 429 になる。
#   取り出し方は tools/README.md の「キーの置き方」を参照。
GEMINI_API_KEY=
