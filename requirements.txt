jinja2>=3.1
requests>=2.31
feedparser>=6.0
markdown>=3.5

# 配信元の記事本文を抜き出す。無いと fulltext.py が本文を1件も取れず、
# 全件が「本文なし」として静かにスキップされる（2026-08-01 に実行機側で
# 発生していた形跡あり）。任意ではなく必須。
trafilatura>=2.0

# 任意: 完全オフライン翻訳（英⇄日）。導入すると収集時に相手言語の訳を自動生成する。
# 外部APIは使わずローカルで動作する。
# argostranslate>=1.9
