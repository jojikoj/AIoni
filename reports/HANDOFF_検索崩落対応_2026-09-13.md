# 引き継ぎ: Google検索の崩落対応（2026-09-13 georgek の MacBook から Mac mini へ）

## いまの状態
- `1745fdb` を main に push 済み: ニュース個別ページを全件 `noindex, follow`・sitemap除外、Zenn/Qiita は `retired` で収集停止。
- **公開はまだ**（9/13 12:30 時点のライブ sitemap は 2,492 URL・うちニュース 2,156）。
  次の日次 `deploy.sh`（origin/main を取り込む）で反映される。ローカルビルドでは sitemap 338 URL。

## Mac mini でやること（残作業）
1. **公開の確認**（日次の後）
   `curl -s https://ai-oni.com/sitemap.xml | grep -c '<loc>https://ai-oni.com/news/'` が 1 以下になっていること。
   2,156 のままなら deploy.sh が 1745fdb を取り込めていない → 手で `./tools/deploy.sh`。
2. **サイトマップの状態確認（翌日以降）**
   `python3 tools/searchconsole_report.py` で「検出URL数」が約340に減ったか見る。
   再送信は必須ではない（Google は送信なしでも毎日 sitemap を読んでいる: 送信 9/04・最終読込 9/13）。
   すぐ反映させたいときだけ Search Console 画面 → サイトマップ → `sitemap.xml` を再送信。
   ※ このツールのトークンは `webmasters.readonly` なので API からの再送信はできない。

## 9/13 に Search Console 画面で確かめた事実（判断の根拠）
- 手動による対策: なし / セキュリティの問題: なし / クロール: ホスト状態「問題なし」（200が97%）。
- 登録済みページ数は崩落前後で横ばい（8/11 1,773 → 9/04 1,760）。**索引落ちではなく順位の抑制**。
- 日次表示（全体/記事/ニュース/論文）: 8/13 80/11/72/2 → 8/14 41/8/26/5 → 8/15 23/0/20/4 → 8/16 5/2/1/2。
  全区分が同じ3日で落ちた。
- **8/13 の title/description 一括書換は原因ではない**: 書換した記事198本 表示294→50（−83%）、
  書換していない40本 65→8（−88%）。（比較期間 7/17–8/13 vs 8/14–9/10）
- 指名検索「aiの鬼」: 表示54・平均5.3位 → 表示2・44.5位。トップページ 7.5位 → 28位。
  → サイト単位の品質評価の低下と判断（大量生成コンテンツ）。

## 判定
- build.py のコメントでは「2026-12 に /articles/ の28日表示が 351 に戻るか」。
- 記事側も絞り込む（無人生成記事の noindex 等）なら記事表示は物差しにならないので、
  **指名検索「aiの鬼」の順位**（現在 40〜75位、崩落前 5位）で判定する方がよい。
- 戻らなければ次に疑うのは 7/19〜20 に一括投入した AI解体新書128本。

## 注意
- 9/13 に別セッション（georgek-d4）が、この上に「無人生成記事189本を googlebot だけ noindex」
  「daily.sh の記事自動公開を AIONI_AUTO_ARTICLES=1 のときだけに」等を push すると連絡してきたが、
  12:40 時点で未 push・セッション終了。やるなら改めて判断すること（未実施）。
